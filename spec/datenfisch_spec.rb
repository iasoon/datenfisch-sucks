require 'spec_helper'

describe Datenfisch do
  it 'has a version number' do
    expect(Datenfisch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
